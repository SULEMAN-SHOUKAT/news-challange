interface Window {
  CONFIG: {
    api: {
      newsApi: {
        url: string;
        auth: string;
      };
      nyTimes: {
        url: string;
        auth: string;
      };
      guardian: {
        url: string;
        auth: string;
      };
    };
  };
}
